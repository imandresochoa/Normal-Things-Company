/** Mirrors `logo-trace` duration in app/globals.css (2.4s). */
export const LOGO_TRACE_MS = 2400;

/** Half of the logo draw — underline may start once this has elapsed. */
export const LOGO_MIDPOINT_MS = LOGO_TRACE_MS / 2;

/** Squiggle underline draw duration (shorter than the logo). */
export const UNDERLINE_TRACE_MS = 700;
